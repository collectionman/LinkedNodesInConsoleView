#include <iostream>
#include "node.h"

/**
 * En este programa se muestra por consola un conjunto
 * de nodos ( puntos en la consola ), que estaran conectados
 * entre si por medio de lineas punteadas ( guiones ), a su vez
 * se mostrara la distancia existente entre cada nodo.
 */


// function to define:
//      int plusDistances( Node* initialNode ) ;
//

int main()
{
    Node node = Node( Coordinate( 13, 4 ) );
    return 0;
}
